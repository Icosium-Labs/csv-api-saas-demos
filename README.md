# csv-api-saas-demos

This repo contains example project that could be reused and customised to interact with your API created in CSV Gateway from CSV files, like for example a quick and easy way to create a prototype of a Web application without having to deal with backend / database hosting.
